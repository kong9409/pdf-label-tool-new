# v13 横向擦除优化说明

本版本针对目的地字段下方的 `FBA:` 行进行了横向同一行擦除：

- 仅擦除 `FBA:` 或 `FBA：` 后面的公司名/地址内容
- 保留 `FBA:` 本身
- 擦除区域只覆盖同一行的横向范围，不向上遮挡“目的地：”，不向下遮挡仓库代码（如 LAS1 / XLX7 等）
- 不限定中文或英文，只要是在目的地 `FBA:` 后面的同一行内容都会被去掉
- SellerSKU 识别支持 `FD-US-...`、`FDM888A-FX-US` 等格式
- 文件名按 `SellerSKU-数量只.pdf` 输出，数量=每页“数量”字段累加
