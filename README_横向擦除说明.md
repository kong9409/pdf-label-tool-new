# v14 横向擦除与标签修复说明

本版本优化点：

1. 目的地 FBA 行采用同一行横向擦除。
2. 只保留 `FBA` 三个字母，删除后面的冒号以及英文、中文、拼音公司名或地址。
3. 不向上、不向下扩大擦除区域，避免遮挡“目的地”和仓库代码。
4. 为防止边缘被误伤，会从原始 PDF 中截取“目的地”和“FBA”原始字形并贴回原位置，尽量做到看不出修补痕迹。
5. SellerSKU 数量命名规则保持：`SellerSKU-数量只.pdf`，数量为每页 `数量` 字段累加。
