# v19 更新说明

本版基于 v17，新增：

1. 文件命名依据可选：SellerSKU 或映射后的仓库 SKU。
2. 支持上传 SKU 映射表，格式建议为：第一列 sellersku，第二列 映射仓库sku。
3. 输出结构改为“同名文件夹 + 同名 PDF”：`SKU-数量只/SKU-数量只.pdf`。
4. 如果选择仓库 SKU 命名，工具会用映射后的仓库 SKU 建立文件夹和文件名；未匹配到则回退使用 SellerSKU。
5. 如果选择仓库 SKU 命名，多个 SellerSKU 映射到同一个仓库 SKU 时，会归入同一个仓库 SKU 输出文件。
